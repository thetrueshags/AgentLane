#!/usr/bin/env bash
set -euo pipefail
common_dir="$(git rev-parse --git-common-dir)"
main_worktree_root="$(cd "$common_dir/.." && pwd)"
if [ -f "$main_worktree_root/bin/board" ]; then
  exec "${AGENTLANE_PYTHON:-python3}" "$main_worktree_root/bin/board" check-push
fi
exec agentlane check-push

# AgentLane v0.1 implementation plan

## Inspection findings

The existing standard-library CLI owns all behavior in `bin/board`; the stdio MCP wrapper
delegates to it. Each clone has a disposable board worktree under Git's common directory.
JSON task, claim and member files and per-member JSONL notes/events live on an orphan `board`
branch. Mutations fetch, replay and retry a fast-forward push. Work uses `claim/*` branches;
landing rebases, checks paths, runs Bash gates and retries competing main pushes. PR review,
TTL expiry, heartbeats, handoffs, hot paths, generated Markdown and optional Slack already exist.
The tests use local bare remotes; the simulator exercises contention and recovery. Seven agent
configurations share the MCP server. MIT and community contribution files are already present.

## Keep

- Git branches, JSON state, standard-library runtime, legacy commands and MCP tool names.
- Existing gates, direct landing, PR mode, notes, hot paths, expiry and simulation coverage.
- Repository-owned configuration and gates; no service, database or agent runtime.

## Gaps to close

- No installable CLI, version, doctor, task details or simple list filters.
- Partial-prefix globs can miss overlaps; state and identifiers lack validation.
- Expiry/render paths can ignore rejected pushes; same-checkout commands can race.
- Main landing and board completion are separate writes; a gate can outlive ownership.
- Missing gates silently pass; hooks can be overwritten; failed join leaves local identity.
- Slack timestamps can skip events from the same second and JSON narration mixes output.
- Documentation overstates automatic heartbeats and some safety guarantees.

## Ordered implementation

1. Package the existing implementation; retain `bin/board`, add the `agentlane` entry point,
   version, command aliases, positional task arguments, `--paths`, list/show and read-only doctor.
2. Harden validation, ownership/expiry, local operation serialization and concurrent claims.
   Make direct main + board updates atomic with Git; preserve PR mode and explicit recovery.
3. Polish installation, gates, events/Slack, MCP and agent integrations. Add deterministic
   failure and contention tests as each behavior changes.
4. Rewrite onboarding and architecture docs, add a three-worker example, changelog and release
   instructions. Add packaging and lint checks to CI.
5. Run the complete suite, installed/wheel smoke checks and a local concurrent simulation;
   inspect the final diff and document remaining limits.

## Compatibility and boundaries

Existing task IDs and schema fields remain readable. New fields are additive. New task IDs may
use `AL-N` allocated inside the board transaction. Direct landing will require remote atomic-push
support; fail clearly rather than downgrade the guarantee. Claims remain cooperative ownership,
not an authorization boundary. PR review/merging remains a hosting workflow. Source maintenance
uses ordinary local commits; this task must not push, publish a release, or post externally.

## Completed implementation

All five stages are implemented. The original engine is now a Python package with thin legacy
launchers; no runtime dependency was added. Read-only inspection and additive setup live in two
small modules. Direct landing and automatic rollback share one atomic two-ref push helper.
Claims have lease tokens and conservative overlap checks. Local branches survive landing so
unpublished work can be repaired after rollback. PR review and GitHub automation remain optional.

The board schema remains compatible, with additive updated timestamps, lease tokens and event
IDs. Existing hot paths, gates, handoffs, review reconciliation and notes remain. Templates and
canonical instructions are checked for consistency. Documentation now distinguishes source
contribution from using a board and explains timing, glob, hosting and recovery limits.

## Validation record

- Editable installation, CLI help/version and isolated wheel installation passed. The wheel
  smoke check installed project files in an unrelated repository and landed its setup task
  through a local bare remote without source-checkout imports or GitHub credentials.
- The three-worker simulation (seed 17) completed six landings, rejected eight overlaps and
  recovered two injected post-merge failures. It found zero overlapping ownership violations,
  lost tasks, unexplained main commits or unexpected worker crashes.
- Dependency-free Python 3.9 syntax, formatting, JSON and template checks passed. All three
  workflow YAML files parsed locally. The CI matrix covers Linux 3.9/3.12 and Windows 3.12;
  its new configuration has not run remotely because publication was excluded.
- Final complete suite: 61 tests passed on Windows/Python 3.12 with
  `python -m unittest discover -v` (197.379 seconds). The repository's
  `agentlane gate --all --json` independently passed all 61 tests (163.998 seconds).
  This includes 21 additional tests beyond the original 40, plus strengthened recovery coverage.
- Final diff and documentation file links passed review. Changes remain in local commits on
  `work/v0.1`; no branch, release, package or message was published for this task.

Remaining boundaries: direct mode needs an atomic-push-capable remote and cooperative workers
with separate clones and clocks/configuration in agreement. Glob overlap can conservatively
refuse disjoint patterns. PR creation and its board receipt cannot be one Git transaction;
inspect both after failure. PR branch history recovery can require manual Git work. Slack is
at-least-once. Live editor/agent clients and hosted PR operations were not exercised locally.
The sample gate must be customized for projects without a recognized test runner.

Potential v0.2 work: idempotent PR-creation recovery, clearer branch-history recovery assistance,
and a wider platform/client integration matrix. Keep these improvements Git-native.
